// @mfkit/kit/healing — Phase 1 Step 7 fills this with:
//   - retry+backoff
//   - quarantine after N failures
//   - runtime singleton version check
//   - manifest validation w/ last-known-good cache
//
// All implementations are wired through the HealingStrategy contract in
// @mfkit/plugin-api so enterprises can swap defaults.

export {};
