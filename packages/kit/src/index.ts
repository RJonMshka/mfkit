// @mfkit/kit — runtime entry. Phase 1 Step 2 fills these with real
// implementations (defineConfig + defineMFE validated pass-throughs).
//
// Subpath entries (./vite, ./react, ./healing) carry the heavy framework-
// specific code so consumers only pay for what they import.

export {};
