# @mfkit/codemods

MFKit's upgrade tooling. Phase 1 ships the infrastructure (registry, CLI stub,
version-manifest format). Real codemods land in v0.3+ when there is a v0.1 →
v0.2 migration to author.

```bash
npx mfkit-migrate           # currently a stub
```
