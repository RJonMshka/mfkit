#!/usr/bin/env node
// mfkit-migrate CLI — Phase 1 Step 10 stubs the entry point so the binary
// exists end-to-end. Real subcommands (`upgrade`, `list`, `dry-run`) land
// alongside the first real codemod in v0.3+.

console.log("mfkit-migrate: alpha stub (Phase 1). No codemods registered yet.");
